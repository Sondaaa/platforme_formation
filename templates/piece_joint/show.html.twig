{% extends 'base.html.twig' %}

{% block title %}PieceJoint{% endblock %}

{% block body %}
    <h1>PieceJoint</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ piece_joint.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ piece_joint.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ piece_joint.description }}</td>
            </tr>
            <tr>
                <th>Fichier</th>
                <td>{{ piece_joint.fichier }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_piece_joint_index') }}">back to list</a>

    <a href="{{ path('app_piece_joint_edit', {'id': piece_joint.id}) }}">edit</a>

    {{ include('piece_joint/_delete_form.html.twig') }}
{% endblock %}
